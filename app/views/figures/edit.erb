<h1>What Would You Like to Edit?</h1>
<form method="POST" action="/figures/<%=@figure.id%>">
    <input type="hidden" id="hidden" name="_method" value="PATCH">
    <label for="figure_name">What is the Figure's Name?:</label>
        <input type="text" default="<%=@figure.name%>" name="figure[name]" id="figure_name"><br>
    <label for="figure_titles">What are the Figure's Titles?:</label>
        <%@titles.each do |title|%>
        <input type="checkbox" name="figure[title_ids][]" value="<%=title.id%>" <%="checked" if @figure.titles.include?(title)%>> <br>
    <label for="new_figure_titles">What is the new Title for the Figure?:</label>
        <input type="text" name=figure[title] id="new_title">
        <%end%>
    <label for="figure_landmarks">What are the Figure's Landmarks?:</label>
        <%@landmarks.each do |landmark|%>
        <input type="checkbox" name="figure[landmark_ids][]" value="<%=landmark.id%>" <%+"checked" if @figure.landmarks.include?(landmark)%>> <br>
        <%end%>
    <label for="figure_new_landmark">What is the new Landmark for the Figure?:</label>
        <input type="text" name="figure[landmark]" id="new_landmark">
    <input type="submit" value="Edit Figure">
</form>